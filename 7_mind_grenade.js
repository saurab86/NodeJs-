const num1 = 5
const num2 = 10

function addValues(){
    console.log(`The sum is ${num1+num2}`)
}

addValues()
// If we have function inside the module that we invoke, we need not used to assign to a variable.
// but must have some executing fuction.